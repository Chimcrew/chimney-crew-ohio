# If the Netlify UI still has "bun run build", change it to "npm run build".
# UI build settings override this file when they are set.
[build]
  command = "npm run build"
  publish = "dist/client"

[build.environment]
  NODE_VERSION = "22"
  NPM_FLAGS = "--no-fund --no-audit"

[dev]
  command = "npm run dev"
  port = 8080

# After the first deploy, add two Form notifications in the Netlify UI:
# 1. Email you at theductorsairduct@gmail.com on every chimcrew-lead submit
# 2. Confirmation email to the submitted "email" field ({{email}})
